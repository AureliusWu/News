# V0.1.1 回归验证记录

**时间：** 2026-09-07  
**路径：** `D:\AI项目\News`  

## 1. 后端测试

- 命令
  - `pytest`（在 `backend` 目录执行）
- 结果
  - 通过：`1 passed`
  - 时长：`45.39s`（含 45 条警告）
  - 备注：测试用例为健康检查接口，包含数据库连接失败退化场景（返回 `healthy` 或 `degraded`）断言。

## 2. 前端测试

- 命令
  - `npm test`（在 `frontend` 目录执行）
- 结果
  - 通过：`1 passed`（1 个测试文件，1 个测试）
  - 备注：使用 `vite test`/`vitest` 运行的 `src/pages/__tests__/HomeView.spec.ts`。

## 3. 前端构建

- 命令
  - `npm run build`（在 `frontend` 目录执行）
- 结果
  - 构建成功
  - 关键产物：
    - `dist/index.html`
    - `dist/assets/index-CLuIKvbk.js`
    - `dist/assets/index-eMCGP3k_.css`
    - PWA 产物 `dist/sw.js`、`dist/workbox-bdb082da.js`

## 4. API / 前端可访问实测

> 由于当前环境未安装 `docker`，改用本机直接启动服务进行实测。

- 启动命令
  - 后端：`uvicorn app.main:app --host 0.0.0.0 --port 8000`（在 `backend`）
  - 前端：`npm run preview -- --host 0.0.0.0 --port 8080`（在 `frontend`）
- 访问验证
  - `GET http://localhost:8000/health`
    - 响应：`status=degraded`，`app_name=Global News`，`db=False`，HTTP 200
  - `GET http://localhost:8000/api/v1/health`
    - 响应：`status=degraded`，`app_name=Global News`，`app_version=0.1.1`，HTTP 200
  - `GET http://localhost:8080/`
    - 响应：HTTP 200，响应体长度 `517`
    - 包含关键前端可见内容 `Global News`

## 5. 风险与说明

- 当前 PostgreSQL 未在宿主机运行，因此健康检查返回 `degraded`（数据库连接失败）是预期降级行为；
  API 与前端入口本身均可达且返回 200。
- `docker compose` 环境在当前机器不可用（`docker` 命令不存在），完整容器链路未通过本机验证。

## 6. 结论

- 本次验证结论：**关键回归验证通过（测试、构建与可访问性均通过），但数据库生产链路验证受宿主环境限制，当前为降级可用状态。**
