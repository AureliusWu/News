# V0.1.1 回归验证记录

**时间：** 2026-09-08  
**路径：** `D:\AI项目\News`  

## 1. 回归验证

### 1.1 后端测试
- 命令
  - `cd backend && python -m pytest`
- 结果
  - 通过：`1 passed`
  - 备注：健康检查返回 `status=degraded`，在无数据库时 `database_connected=false`。

### 1.2 前端测试
- 命令
  - `cd frontend && npm test -- --watch=false`
- 结果
  - 通过：`1 passed`。

### 1.3 前端构建
- 命令
  - `cd frontend && npm run build`
- 结果
  - 构建成功（含 PWA 产物，能够生成 `dist`）。

### 1.4 API / 前端可访问实测
- 启动方式（本次为避开容器链路）：`uvicorn` + `npm run dev`
- 实测结果
  - `GET http://127.0.0.1:8000/health`
    - `HTTP 200`，`status=degraded`，`db=False`
  - `GET http://127.0.0.1:8000/api/v1/health`
    - `HTTP 200`，`status=degraded`，`app_name=Global News`，`app_version=0.1.1`
  - `GET http://127.0.0.1:8080`
    - `HTTP 200`，返回前端 HTML 内容

## 2. Docker 修复与可访问性

- 已完成
  - `docker compose` 可用，版本输出：`Docker Compose version v5.5.0`
  - 恢复后关键可用性
    - `docker info --format "Server {{.ServerVersion}}"` 返回 `Server 29.7.2`
    - `docker context ls` 显示 `desktop-linux` 存在且可选
- 处置过程（现场修复）
  - 清理并重建 `C:\Users\84046\AppData\Local\Docker\run` 异常 socket/reparse 残留（`sailor-ingest.sock` 等）。
  - 清理 `C:\Users\84046\AppData\Local\docker-secrets-engine\engine.sock` 相关残留并重命名目录规避。
  - 启动 `Docker Desktop` 并观察 backend 日志，确认 `engine linux/wsl is running`。

## 3. GitHub 仓库与同步

- 仓库：`AureliusWu/News`
- 远端：`origin https://github.com/AureliusWu/News.git`
- 可见性：已确认 `PUBLIC`
- 仓库同步：`git status` 与 `git push` 验证后进行后续提交动作

## 4. 结论

- **关键回归验证已完成**，Docker 与应用可访问性链路已恢复到可复现状态，后续可继续进行容器链路验证（如需要）。
