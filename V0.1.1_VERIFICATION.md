# V0.1.1 回归验证记录

**时间：** 2026-09-07  
**路径：** `D:\AI项目\News`  

## 1. 后端测试

- 命令
  - `cd backend && python -m pytest`  
- 结果
  - 通过：`1 passed`
  - 时长：`3.69s`（含 45 条警告）
  - 备注：健康检查用例通过，默认在无数据库场景下可返回 `degraded`。

## 2. 前端测试

- 命令
  - `npm test`（在 `frontend` 目录执行）
- 结果
  - 通过：`1 passed`（1 个测试文件，1 个测试）
  - 备注：基于 `vitest` 执行 `src/pages/__tests__/HomeView.spec.ts`。

## 3. 前端构建

- 命令
  - `npm run build`（在 `frontend` 目录执行）
- 结果
  - 构建成功
  - 关键产物：
    - `dist/index.html`
    - `dist/assets/index-CLuIKvbk.js`
    - `dist/assets/index-eMCGP3k_.css`
    - PWA 产物 `dist/sw.js`、`dist/workbox-bdb082da.js`

## 4. API / 前端可访问实测

> 使用本机直接启动服务进行实测（避免 `docker` 引擎阻塞影响）：

- 启动命令
  - 后端：`python -m uvicorn app.main:app --host 127.0.0.1 --port 8000`（在 `backend`）
  - 前端：`npm run dev -- --host 127.0.0.1 --port 8080`（在 `frontend`）
- 访问验证
  - `GET http://127.0.0.1:8000/`
    - 响应：`{"name":"Global News","version":"0.1.1","environment":"development","status":"ok"}`
    - HTTP 200
  - `GET http://localhost:8000/health`
    - 响应：`status=degraded`，`app_name=Global News`，`db=False`，HTTP 200
  - `GET http://localhost:8000/api/v1/health`
    - 响应：`status=degraded`，`app_name=Global News`，`app_version=0.1.1`，HTTP 200
  - `GET http://localhost:8080/`
    - 响应：HTTP 200，`Content-Type: text/html`，响应体长度 `354`
    - 包含关键前端可见内容 `<html lang="en">` 起始片段

## 5. Docker 修复与阻塞

- 修复结果（已完成）
  - 初始：`docker compose` 命令不存在，`docker info` 提示 daemon 未启动
    - `docker --version` 有效、`docker compose` 报 `unknown command`
  - 已执行修复：
    - 将 `docker-compose.exe` 从 `C:\Program Files\Docker\Docker\resources\cli-plugins\` 复制到用户级插件目录 `C:\Users\84046\.docker\cli-plugins\`
    - 复测：`docker compose version` 输出 `Docker Compose version v5.5.0`
- 当前阻塞（未完全解决）
  - `docker info` 仍报：
    - `failed to connect ... open //./pipe/dockerDesktopLinuxEngine: The system cannot find the file specified.`
  - 日志与命令显示 WSL 组件未正确安装/启用：
    - `wsl --status` 输出安装提示
    - `wsl --install --no-launch` 在当前权限下失败
  - `docker desktop start/status` 无法拿到运行态
  - 结论：**`compose` 命令可用，但引擎仍需 WSL 环境支持才能完整启动**

## 6. 结论

- 本次验证结论：**关键回归验证已完成（后端测试、前端测试、前端构建、API+前端可访问）**。  
  `docker` 部分：已完成 `compose` 命令修复，但因本机 WSL 未装配，Docker 引擎仍未可用，容器链路自动化尚未落地。
